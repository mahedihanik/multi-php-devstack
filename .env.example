# Copy to .env  (cp .env.example .env)

# MySQL credentials (shared by all projects)
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=app
MYSQL_USER=app
MYSQL_PASSWORD=app

# Host timezone applied to all PHP containers
TZ=UTC
